.vscode
# Artefatos derivados de build/teste .NET — nunca versionar (task 3.0: evita
# que .cs gerados em obj/ entrem no escopo do `dotnet format` do gate).
[Bb]in/
[Oo]bj/
TestResults/
playwright-report/
blob-report/
test-results/
# Artefatos derivados do frontend (task 7.0) — nunca versionar node_modules/dist.
node_modules/
dist/
# Logs/PIDs locais de scripts/dev-up.sh — nunca versionar.
.dev/
.env