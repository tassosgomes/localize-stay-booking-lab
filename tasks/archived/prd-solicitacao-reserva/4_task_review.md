# Review — Task 4.0: Preparar integração frontend tipada com o contrato de Booking (EN-FE-01)

- **Mode:** focused (primeira revisão)
- **Resultado:** `VALIDATION APPROVED` — 0 bloqueantes, 4 recomendações não bloqueantes
- **HEAD revisado:** `65ea51f` (checkpoint 3.0) + unstaged changes da task; árvore estável antes/depois da revisão
- **Escopo do diff:** 7 criados (`booking.ts` gerado 347L, `apiClient.ts`, `reservationApi.ts` + teste 14 testes, `src/test/mocks/{handlers,server}.ts`, `.env.example`), 4 modificados (`package.json`, `package-lock.json`, `env.ts`, `.env.development`). Alterações em `4_task.md` (status) e `flow-state.json` são gestão de fluxo, não código.

## Gate e evidências executadas (por este validator)

| Comando | Resultado |
|---|---|
| `scripts/ai-flow/gate.sh --filter="reservationApi.test"` | `GATE: APROVADO`, exit 0 — filtro seleciona `reservationApi.test` (14 testes), `tsc --noEmit` ok |
| `cd frontend/localize-stay-frontend && npm run api:generate` | exit 0 |
| `git diff --exit-code -- src/services/api/generated/booking.ts` (após regenerar) | exit 0 — **zero drift**, arquivo byte-idêntico |
| `npm run test` (suíte completa do frontend) | 16/16 — 14 novos + 2 da fundação (`ServiceStatus`), sem regressão |
| `npm run build` | exit 0 (tsc --noEmit && vite build) |
| `git rev-parse HEAD` antes/depois | `65ea51f` inalterado durante a revisão |

## Verificação dos pontos de encomenda

### (a) `guestReference` no body, nunca em header — OK

`reservationApi.test.ts:55-78` captura o `Request` real via MSW e assere: `POST`, pathname `/v1/reservations`, `content-type: application/json`, `guestReference`/`guest-reference`/`x-guest-reference` todos `null` nos headers, e `body === validInput` (com `guestReference` presente). `apiClient.ts:21-24` só injeta `Accept`/`Content-Type` — nenhum header de identidade. Conforme techspec §Estratégia de Fetching (L190-193).

### (b) 7 cenários tipados com discriminação segura — OK

Mapeamento em `reservationApi.ts:69-97` segue a tabela autoritativa da techspec §Tratamento Centralizado de Erros e da task (L167-173):

- `201` → `success` com `Reservation` tipada; o tipo `ReservationResponse` do contrato é alias de `Reservation` (`booking.ts:127`), sem DTO paralelo.
- `400` exige `code === 'VALIDATION_ERROR'` → `malformed`; `422` com guard `isRejectionCode` (5 codes do contrato; `CODE_NOVO` fora do contrato → `failed`, testado em L149-163); `503` + `CATALOG_INDISPONIVEL` → `unavailable` com `traceId?`; `500`/não contratual → `failed` com `status?`/`traceId?`.
- Robustez além do mínimo: 201 com corpo não-JSON → `failed` (L165-179); resposta não-JSON 418 → `failed` com status (L140-147); falha de rede → `failed` sem status (L131-138). `parseProblemDetails` (L40-50) nunca lança; `optionalString` valida `traceId`. União discriminada por `kind` — sem exceção não tratada ao chamador.

### (c) Handlers MSW compartilhados para 5.0/6.0 — OK

`handlers.ts` exporta handler default de 201 (eco de payload + Location), factories por cenário (`reservationMalformedHandler`, `reservationRejectedHandler(code)`, `catalogUnavailableHandler(traceId?)`, `reservationInternalErrorHandler(traceId?)`), fixture tipada `RESERVATION_CREATED_FIXTURE` e URLs (`BOOKING_API_BASE_URL`, `RESERVATIONS_URL`). `server.ts` é `setupServer(...handlers)` reutilizável; comentário orienta adicionar handlers de Catalog sem substituir. `it.each([...REJECTION_CODES])` cobre os 5×422 a partir da lista exportada pelo adapter — 5.0/6.0 consumirão exatamente estes artefatos, sem segundo parser nem segundo conjunto de mocks.

### (d) Tipos gerados sem drift e sem edição manual — OK

`booking.ts` preserva header `auto-generated by openapi-typescript` (L1-4); regeneração produziu arquivo idêntico (`git diff --exit-code` = 0). Script `api:generate` aponta exclusivamente para `tasks/prd-solicitacao-reserva/api-contract.yaml` (não havia script de Catalog a estender — `prd-cadastro-property` ainda não implementada). `openapi-typescript` fixado exato (`7.13.0`); `msw ^2.10.0` já existia da fundação; lock +267/-0 (só o pacote novo, sem upgrades amplos).

### (e) `VITE_BOOKING_API_URL` coerente — OK

`env.ts` expõe `getBookingApiUrl()` via `required()` (throw se ausente/vazia, validação em tempo de chamada — padrão da fundação), distinta de `VITE_BOOKING_URL` (health, sem `/v1`) e de `VITE_CATALOG_URL`. `.env.example` documenta `http://localhost:5102/v1` e o mock Prism `:4010/v1` (techspec L284); `.env.development` fixa `http://localhost:5102/v1`. Teste usa `vi.stubEnv` — prova que o adapter lê a variável em runtime.

### (f) As 4 adaptações do implementer — todas conformes, nenhuma bloqueante

1. **`vitest.setup.ts` da fundação em vez de criar `src/test/setup.ts`:** `vite.config.ts:13` já referencia `./vitest.setup.ts` (jest-dom). O ciclo MSW (listen/`resetHandlers`/close) ficou declarado no próprio arquivo de teste, tornando-o robusto sob a invocação do gate a partir da raiz (onde o config do frontend não carrega) — o requisito substantivo (reset por teste, `onUnhandledRequest: 'error'` provando ausência de rede real) está atendido.
2. **Acessor dedicado `getBookingApiUrl()`:** atende "expor e validar" sem obrigar as 3 variáveis de health; coerente com o comentário "cada feature valida somente a variável do serviço que consome".
3. **`// @vitest-environment node` no teste do adapter:** teste é de transporte (sem DOM), mesmo realm do `fetch` interceptado; funciona tanto via `npm run test` (jsdom default) quanto via gate.
4. **`AbortError` repropagado (`reservationApi.ts:92-95`):** necessário e documentado — a techspec exige que a desmontagem da página (5.0) cancele a requisição pendente; engolir `AbortError` como `failed` faria a UI exibir falha em cancelamento deliberado. Não contradiz "sem exceção não tratada", que visa falha de rede/parse.

### (g) Nenhum escopo de 5.0/6.0 — OK

`features/reservation-request/` contém somente `api/` (sem `index.ts` público, sem página, sem formulário, sem `reservationErrorMapping`, sem E2E). Nenhuma rota/`App`/`vite.config` alterada.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueantes)

1. **Ciclo MSW por arquivo:** quando 5.0 criar testes de UI, replicar o padrão `listen`/`resetHandlers`/`close` + `onUnhandledRequest: 'error'` (ou extrair helper compartilhado em `src/test/`) para evitar divergência entre arquivos.
2. **`AbortError` na 5.0:** registrar na task 5.0 que a página deve capturar o `AbortError` repropagado no cancelamento por desmontagem (senão vira unhandled rejection na UI).
3. **Lista de codes duplicada:** os 5 codes de 422 existem em dois lugares (`REJECTION_CODES` no adapter e `ReservationRejectionCode` em `handlers.ts`). Hoje ambos são cobertos pelo `it.each`; ao consumir em 5.0, importar do adapter para manter fonte única.
4. **Coverage extra já útil:** manter os testes de corpo 201 não-JSON e code fora do contrato como regressão de robustez quando o contrato evoluir (nova versão do `api-contract.yaml` regenera tipos e o drift check acusa).

## Rastreabilidade

- Task 4.0 cobre EN-FE-01 (techspec L426) e EN-01 do inventário (L303): tipos gerados, `VITE_BOOKING_API_URL`, adapter `POST /v1/reservations`, handlers MSW dos 7 cenários — todos presentes e verificados.
- Critérios de sucesso da task (L203-215): todos atendidos com evidência acima; seletor focado sem componentes de UI; nenhum artefato de task futura necessário para compilar/validar.
